name: Hello World
on: [push]
defaults:
  run:
    shell: bash
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
      - uses: actions/checkout@v3

      - name: Build
      - run: ./hello
      - name: Run
      - run: gcc hello.c
